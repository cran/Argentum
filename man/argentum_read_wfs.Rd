% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wfs.R
\name{argentum_read_wfs}
\alias{argentum_read_wfs}
\title{Read a WFS layer as an sf object}
\usage{
argentum_read_wfs(
  x,
  layer,
  bbox = NULL,
  crs = NULL,
  max_features = NULL,
  page_size = arg_opt("argentum.page_size"),
  filter = NULL,
  quiet = arg_opt("argentum.quiet")
)
}
\arguments{
\item{x}{An organization name from \code{\link[=argentum_organizations]{argentum_organizations()}}, or a WFS URL.}

\item{layer}{Layer (feature type) name, as reported by \code{\link[=argentum_layers]{argentum_layers()}}.}

\item{bbox}{Optional spatial filter. Either a numeric vector
\code{c(xmin, ymin, xmax, ymax)}, or any object accepted by \code{\link[sf:st_bbox]{sf::st_bbox()}},
such as an existing \code{sf} object.}

\item{crs}{Coordinate reference system to request, e.g. \code{"EPSG:4326"} or
\code{4326}. \code{NULL} (default) negotiates one from the layer's declared list,
preferring \code{EPSG:4326}; if the layer declares none, the server's own
default is used.}

\item{max_features}{Stop after this many features. \code{NULL} (default) reads
everything.}

\item{page_size}{Features per request. Defaults to
\code{getOption("argentum.page_size")}. Set to \code{Inf} to disable paging for
servers that mishandle \code{startIndex}.}

\item{filter}{A CQL filter pushed to the server, e.g.
\code{"provincia = 'Mendoza'"}. Supported by GeoServer; ignored by some
ArcGIS deployments.}

\item{quiet}{Suppress progress messages.}
}
\value{
An \code{sf} data frame. Zero rows if the filter matches nothing.
}
\description{
Improves on the v1 importer in four ways: the protocol version is taken
from the negotiated capabilities rather than hard-coded, large layers are
fetched page by page instead of in one request that times out, a spatial
or attribute filter can be pushed down to the server, and if the endpoint
cannot produce GeoJSON the request falls back to GML.
}
\details{
When a read fails outright the request is retried with paging disabled and
then against each older protocol version, exactly as the ArgentinaGeoServices
QGIS plugin does: several Argentine GeoServer installs advertise 2.0.0 and
then fail to serialise GML in that version while answering 1.0.0 perfectly.
If every attempt fails, the server is asked for a single feature so that its
own error message can be reported instead of a bare parse failure.
}
\examples{
\dontrun{
url <- "https://wms.ign.gob.ar/geoserver/ows"

# Everything in a small layer
provinces <- argentum_read_wfs(url, "ign:provincia")

# Only what falls inside a bounding box, reprojected
argentum_read_wfs(
  url, "ign:provincia",
  bbox = c(-59, -35, -57, -34),
  crs = 4326
)
}
}
\seealso{
\code{\link[=argentum_download]{argentum_download()}} to write layers straight to disk.
}
