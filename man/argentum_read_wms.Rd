% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wms.R
\name{argentum_read_wms}
\alias{argentum_read_wms}
\title{Render a WMS layer as a raster}
\usage{
argentum_read_wms(
  x,
  layer,
  bbox,
  crs = NULL,
  width = NULL,
  height = NULL,
  style = NULL,
  format = NULL,
  transparent = TRUE,
  quiet = arg_opt("argentum.quiet")
)
}
\arguments{
\item{x}{An organization name from \code{\link[=argentum_organizations]{argentum_organizations()}}, or a WMS URL.}

\item{layer}{Layer name, or a character vector of names to be drawn in
order, first at the bottom.}

\item{bbox}{Extent to render, as \code{c(xmin, ymin, xmax, ymax)} or any object
accepted by \code{\link[sf:st_bbox]{sf::st_bbox()}}. Required: WMS has no "everything" request.}

\item{crs}{CRS of \code{bbox} and of the output, e.g. \code{"EPSG:4326"} or \code{4326}.
Defaults to \code{"EPSG:4326"}, or to the CRS of \code{bbox} if it carries one.}

\item{width, height}{Image size in pixels. If only one is given, the other
is derived from the aspect ratio of \code{bbox}. If neither is given, \code{width}
defaults to 800.}

\item{style}{Optional named style, or one per layer.}

\item{format}{MIME type to request. \code{NULL} (default) negotiates one from the
formats the service advertises for \code{GetMap}, preferring PNG, which keeps
transparency. Pass a value such as \code{"image/jpeg"} to force it.}

\item{transparent}{Request a transparent background.}

\item{quiet}{Suppress progress messages.}
}
\value{
A \link[terra:SpatRaster-class]{terra::SpatRaster} with the CRS set, ready to combine with
vector data read via \code{\link[=argentum_read_wfs]{argentum_read_wfs()}}.
}
\description{
Issues a \code{GetMap} request and returns the rendered image georeferenced as a
\link[terra:SpatRaster-class]{terra::SpatRaster}. Unlike a WFS read this gives you the server's own
cartography — useful for basemaps, orthophotos and any layer published only
as a picture.
}
\examples{
\dontrun{
r <- argentum_read_wms(
  "https://wms.ign.gob.ar/geoserver/ows",
  layer = "ign:provincia",
  bbox  = c(-59, -35, -57, -34),
  width = 800
)
terra::plotRGB(r)
}
}
\seealso{
\code{\link[=argentum_wms_legend]{argentum_wms_legend()}} for the matching legend graphic.
}
