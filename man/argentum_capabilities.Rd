% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capabilities.R
\name{argentum_capabilities}
\alias{argentum_capabilities}
\title{Retrieve and parse a GetCapabilities document}
\usage{
argentum_capabilities(
  x,
  service = c("wfs", "wms"),
  version = NULL,
  refresh = FALSE
)
}
\arguments{
\item{x}{An organization name from \code{\link[=argentum_organizations]{argentum_organizations()}}, or a service
URL. Any query string on the URL is discarded and rebuilt.}

\item{service}{\code{"wfs"} or \code{"wms"}.}

\item{version}{Force a protocol version, e.g. \code{"1.1.0"}. If \code{NULL}
(default) versions are tried newest-first.}

\item{refresh}{Ignore the cache and re-request the document.}
}
\value{
An object of class \code{argentum_capabilities}: an \code{xml_document} with
\code{service}, \code{version} and \code{url} attributes.
}
\description{
Negotiates the protocol version by trying the newest first and falling back,
which is what makes the package work against both GeoServer and ArcGIS
Server deployments. The parsed document is cached; see
\code{\link[=argentum_cache_path]{argentum_cache_path()}}.
}
\examples{
\dontrun{
cap <- argentum_capabilities("https://wms.ign.gob.ar/geoserver/ows", "wfs")
cap
}
}
