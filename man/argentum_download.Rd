% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{argentum_download}
\alias{argentum_download}
\title{Download WFS layers to disk}
\usage{
argentum_download(
  x,
  layers = NULL,
  dir = file.path(tempdir(), "argentum"),
  format = c("gpkg", "geojson", "shp"),
  overwrite = FALSE,
  bbox = NULL,
  crs = NULL,
  max_features = NULL,
  quiet = arg_opt("argentum.quiet")
)
}
\arguments{
\item{x}{An organization name from \code{\link[=argentum_organizations]{argentum_organizations()}}, or a WFS URL.}

\item{layers}{Character vector of layer names. \code{NULL} (default) downloads
every layer the service publishes.}

\item{dir}{Destination directory. Created if absent. Defaults to a session
temporary directory, per CRAN policy on writing to the user's filespace.}

\item{format}{One of \code{"gpkg"} (default), \code{"geojson"} or \code{"shp"}.
GeoPackage is recommended: Shapefile truncates field names to ten
characters and cannot hold more than 2 GB.}

\item{overwrite}{Overwrite files that already exist.}

\item{bbox}{Optional spatial filter. Either a numeric vector
\code{c(xmin, ymin, xmax, ymax)}, or any object accepted by \code{\link[sf:st_bbox]{sf::st_bbox()}},
such as an existing \code{sf} object.}

\item{crs}{Coordinate reference system to request, e.g. \code{"EPSG:4326"} or
\code{4326}. \code{NULL} (default) negotiates one from the layer's declared list,
preferring \code{EPSG:4326}; if the layer declares none, the server's own
default is used.}

\item{max_features}{Stop after this many features. \code{NULL} (default) reads
everything.}

\item{quiet}{Suppress progress messages.}
}
\value{
Invisibly, a data frame with one row per layer and columns \code{layer},
\code{status} (\code{"success"}, \code{"skipped"} or \code{"error"}), \code{path}, \code{features} and
\code{message}.
}
\description{
Writes one file per layer and returns a report. Failures are recorded and
the run continues, so one broken layer does not abort a bulk download.
}
\examples{
\dontrun{
report <- argentum_download(
  "https://wms.ign.gob.ar/geoserver/ows",
  layers = "ign:provincia",
  dir = tempdir()
)
subset(report, status == "error")
}
}
