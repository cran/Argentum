% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{argentum_organizations}
\alias{argentum_organizations}
\title{List organizations publishing OGC services}
\usage{
argentum_organizations(
  service = c("any", "wfs", "wms", "wcs", "csw"),
  refresh = FALSE
)
}
\arguments{
\item{service}{Keep only organizations offering this service: \code{"any"}
(default), \code{"wfs"}, \code{"wms"}, \code{"wcs"} or \code{"csw"}. The package can read WMS
and WFS; \code{wcs_url} and \code{csw_url} are carried so you can see what an
organization publishes, and reach it with another tool.}

\item{refresh}{If \code{TRUE}, ignore the cache and re-download the catalogue.}
}
\value{
A data frame with columns \code{name}, \code{level}, \code{jurisdiction},
\code{dependency}, \code{organization}, \code{updated}, \code{wms_url}, \code{wfs_url}, \code{wcs_url}
and \code{csw_url}. \code{name} is the human-readable label used by the rest of the
package.
}
\description{
Returns the catalogue of Argentine public bodies known to expose OGC
geoservices, as published in IDERA's own spreadsheet.
}
\examples{
\dontrun{
orgs <- argentum_organizations()
head(orgs$name)

# Only those with a usable WMS endpoint
argentum_organizations(service = "wms")
}
}
\seealso{
\code{\link[=argentum_search_organizations]{argentum_search_organizations()}}, \code{\link[=argentum_layers]{argentum_layers()}}
}
